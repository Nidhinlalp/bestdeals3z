// Web-specific TypeScript types
// For shared types across apps, use @ecom/types instead

// TODO: Add web-specific types as features are built
// export type { SearchFilters, SortOption, ProductListParams } from './search';
// export type { CartUIState } from './cart';

export {};
